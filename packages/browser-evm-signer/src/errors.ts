// Errors live in the shared core. Re-exported from here for backwards compatibility with
// existing consumers (and intra-package imports).
export { findWrongWalletAddressError, SignerErrorCode, WrongWalletAddressError } from "wallet-signer-core";
